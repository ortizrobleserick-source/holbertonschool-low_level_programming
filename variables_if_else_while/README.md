# Variables, if, else, while

Este directorio contiene ejercicios en C para practicar **variables**, **estructuras condicionales (`if` / `else`)** y **bucles `while`**.

## Compilación

Para compilar cualquier programa:

```bash
gcc -Wall -Werror -Wextra -pedantic -std=gnu89 archivo.c -o ejecutable